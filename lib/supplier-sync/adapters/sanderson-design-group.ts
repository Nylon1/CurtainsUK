import type { SupplierAdapter } from "../types";

/** Deliberately type-only: a future implementation only needs the normalized adapter contract. */
export type SandersonDesignGroupAdapter = SupplierAdapter;
