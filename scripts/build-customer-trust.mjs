import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {version,groups,pages,aliases,contacts,changeNotice,imageNotice} from '../content/customer-trust/catalogue.mjs';
const theme='shopify-theme/curtainsuk-new-design-live-base';
const out='artifacts/customer-trust-v1';
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const css=await readFile('content/customer-trust/footer.css','utf8');
const js=await readFile('content/customer-trust/footer.js','utf8');
const strings={}; let count=0;
const text=(s,liquid)=>{if(!liquid)return esc(s);const key='copy_'+(++count);strings[key]=s;return `{{ 'customer_trust.footer.${key}' | t }}`;};
function footer(liquid=false,standalone=false){
 const t=s=>text(s,liquid);
 const link=([label,url])=>url==='#cookie-settings' ? (standalone?`<a href="/pages/cookies-policy#cookie-settings">${t(label)}</a>`:`<button type="button" id="cookie-settings" data-cookie-settings>${t(label)}</button>`) : `<a href="${esc(url)}">${t(label)}</a>`;
 return `<section class="cuktrust" data-cuk-trust-footer aria-label="${t('CurtainsUK help and information')}" data-cookie-unavailable="${t('Cookie preferences could not be opened. No choice has been changed. Please try again or contact support@curtainsuk.com for help.')}"><div class="cuktrust__wrap"><p class="cuktrust__eyebrow">${t('NEED SOME HELP?')}</p><h2>${t('We’re here from inspiration to installation.')}</h2><nav class="cuktrust__service" aria-label="${t('CurtainsUK help')}">${[['Measure','Guided Measure','/pages/how-to-measure'],['Fit','How to Fit','/pages/how-to-fit'],['Samples','Order Samples','/pages/samples'],['Advice','Contact Us','/pages/contact-us'],['Privacy','Room-image privacy','/pages/room-image-ai-privacy']].map(([small,label,url])=>`<a href="${url}"><small>${t(small)}</small><strong>${t(label)} <span aria-hidden="true">→</span></strong></a>`).join('')}</nav><nav class="cuktrust__groups" aria-label="${t('Customer care and policies')}">${groups.map(([label,links])=>`<details data-trust-group open><summary>${t(label)}</summary><ul>${links.map(entry=>`<li>${link(entry)}</li>`).join('')}</ul></details>`).join('')}</nav><p role="status" class="cuktrust__status" data-cookie-status></p><div class="cuktrust__contact" id="curtainsuk-help">${[['Customer support',contacts.support],['Complaints',contacts.complaints],['Media & Press',contacts.media]].map(([label,email])=>`<p><strong>${t(label)}</strong><a href="mailto:${email}">${t(email)}</a></p>`).join('')}</div><div class="cuktrust__business"><p>${t('Apex Curtains Ltd trading as Curtains UK')}</p><p>${t('Customer contact and authorised returns: 36–44 Bolton Road, Blackburn, BB2 3FA')}</p><p>${t('Company 15652466')}</p><p>${t('Made-to-measure order-change requests:')} <a href="mailto:${contacts.changes}">${t(contacts.changes)}</a></p></div></div></section>`;
}
const body=page=>page.sections.map(([title,copy])=>`<h2>${esc(title)}</h2>${copy.startsWith('<p>')?copy:`<p>${copy}</p>`}`).join('\n');
const sitemap={key:'sitemap',title:'Sitemap',url:'/pages/sitemap',action:'CREATE',status:'READY FOR OWNER REVIEW',confirmations:[],sections:groups.map(([title,links])=>[title,links.filter(([,url])=>!url.startsWith('#')).map(([label,url])=>`<a href="${esc(url)}">${esc(label)}</a>`).join('<br>')])};
const allPages=[...pages,sitemap];
await mkdir(out+'/pages',{recursive:true});await mkdir(out+'/shopify-policies',{recursive:true});await mkdir('lib/storefront/customer-trust',{recursive:true});
const footerLiquid=footer(true);
await writeFile(theme+'/snippets/curtainsuk-trust-footer.liquid',`{% doc %}\nCanonical customer trust footer. Generated by scripts/build-customer-trust.mjs.\n{% enddoc %}\n${footerLiquid}\n{% stylesheet %}\n${css}{% endstylesheet %}\n{% javascript %}\n${js}{% endjavascript %}\n`);
const localePath=theme+'/locales/en.default.json';const localeSource=await readFile(localePath,'utf8');const localeHeader=localeSource.slice(0,localeSource.indexOf('{'));const locale=JSON.parse(localeSource.slice(localeSource.indexOf('{')));locale.customer_trust={footer:strings,colour:{note:'Screens, light and fabric surfaces affect colour. A physical sample helps you judge it in your room.',link:'Fabric Colour, Shade & Variation'}};await writeFile(localePath,localeHeader+JSON.stringify(locale,null,2)+'\n');
const footerHtml=`<style>${css}</style>${footer(false,true)}<script>${js}</script>`;
await writeFile('lib/storefront/customer-trust/generated.ts',`// Generated from content/customer-trust. Do not hand edit.\nexport const trustFooterHtml = ${JSON.stringify(footerHtml)};\nexport const roomImageNotice = ${JSON.stringify(imageNotice)};\nexport const roomPrivacyHtml = ${JSON.stringify(body(pages.find(p=>p.key==='room-image-ai-privacy')))};\n`);
const pageCss='body{margin:0;background:#f8f6ef;color:#173d35;font:16px/1.7 Arial,sans-serif}a{color:inherit;text-underline-offset:4px}header{padding:22px clamp(24px,5vw,74px);border-bottom:1px solid #d4d8cd;display:flex;justify-content:space-between;gap:16px;flex-wrap:wrap}header a{min-height:44px;display:inline-flex;align-items:center}main{max-width:830px;margin:auto;padding:48px 24px 72px}h1{font:normal clamp(36px,5vw,58px)/1.1 Georgia,serif}main h2{font:normal 26px/1.2 Georgia,serif;margin-top:36px}.review{background:#ede7d9;padding:16px;margin:24px 0;font-size:14px}.review li{margin:8px 0}li a{overflow-wrap:anywhere}a:focus-visible{outline:2px solid #173d35;outline-offset:4px}.index{display:grid;gap:16px;padding:0;list-style:none}.index a{display:block;border-bottom:1px solid #c7cbbd;padding:12px 0}';
const wrap=(title,content)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${esc(title)} — CurtainsUK review</title><style>${pageCss}${css}</style></head><body><header><a href="/">CurtainsUK · Customer trust review</a><span>Unpublished · ${version}</span></header>${content}${footer()}<script>${js}</script></body></html>`;
for(const page of allPages){
 const warning=`<aside class="review"><strong>${esc(page.status)} · Unpublished draft</strong>${page.confirmations.length?`<ul>${page.confirmations.map(s=>`<li>NEEDS CONFIRMATION — ${esc(s)}</li>`).join('')}</ul>`:''}</aside>`;
 await writeFile(`${out}/pages/${page.key}.html`,wrap(page.title,`<main><h1>${esc(page.title)}</h1>${warning}${body(page)}</main>`));
 if(page.url.startsWith('/policies/'))await writeFile(`${out}/shopify-policies/${page.key}.html`,body(page)+'\n');
}
await writeFile(out+'/index.html',wrap('Customer trust V1',`<main><p>FOR OWNER REVIEW</p><h1>Here for every part of your curtain journey.</h1><p>The existing service footer, extended with one shared customer-care and policy system.</p><aside class="review">No Shopify policies, notifications, privacy settings or live theme have been changed. Drafts marked NEEDS CONFIRMATION must not be published.</aside><ul class="index">${allPages.map(p=>`<li><a href="${p.url}">${esc(p.title)}<br><small>${p.status}</small></a></li>`).join('')}</ul></main>`));
await writeFile(out+'/footer.html',wrap('Footer',`<main><p>SHARED FOOTER REVIEW</p><h1>One place for care and clarity.</h1><p>Preview of the canonical footer beneath the current service layer. Links lead to review drafts on this local server.</p></main>`));
await writeFile(out+'/publication-manifest.json',JSON.stringify({version,releaseApproved:false,globalSettingsUnchanged:true,pages:allPages.map(p=>({...p,body:body(p),sections:undefined})),aliases,requiredSurfaces:['Shopify shopPolicies (global, including checkout)','Shopify page records','Shared theme footer','HCI app-proxy footer','Shopify order-confirmation notification','Cookie banner and pixel consent']},null,2)+'\n');
console.log(`Built ${allPages.length} review destinations and canonical Liquid/app-proxy footer. No publication performed.`);
if(process.argv.includes('--release'))throw new Error('RELEASE_BLOCKED: owner review, unresolved facts and global policy/consent verification required');
